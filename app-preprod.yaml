apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: familie-dokument
  namespace: default
  labels:
    team: teamfamilie

spec:
  image: {{ image }}
  port: 8082
  liveness:
    path: /familie/alene-med-barn/mellomlagring/internal/status/isAlive
    initialDelay: 30
    failureThreshold: 10
  readiness:
    path: /familie/alene-med-barn/mellomlagring/internal/status/isAlive
    initialDelay: 30
    failureThreshold: 10
  prometheus:
    enabled: true
    path: /internal/prometheus
  vault:
    enabled: true
  replicas:
    min: 2
    max: 4
    cpuThresholdPercentage: 50
  resources:
    limits:
      memory: 1024Mi
      cpu: "1"
    requests:
      memory: 512Mi
      cpu: 500m
  ingresses: # Optional. List of ingress URLs that will route HTTP traffic to the application.
    - https://familie-dokument.nais.oera-q.local
    - https://familie-dokument.dev-sbs.nais.io
    - https://www-q0.nav.no/familie/alene-med-barn/mellomlagring
  secureLogs:
    enabled: true
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: preprod
