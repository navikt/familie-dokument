server:
  port: 8082
  servlet:
    context-path: /
cors:
  allowed_origins:
    - "http://localhost:3000"

TOKEN_X_ISSUER: http://localhost:${mock-oauth2-server.port}/tokenx
TOKEN_X_JWKS_URI: http://localhost:${mock-oauth2-server.port}/tokenx/jwks
TOKEN_X_CLIENT_ID: familie-app

FAMILIE_DOKUMENT_STORAGE_ENCRYPTION_PASSWORD: not-a-real-pw
FAMILIE_DOKUMENT_FNR_SECRET_SALT: detteErIkkeSaltMenHemmeligSalt

gcp.storage.bucketname: not-a-real-name

spring.main.allow-bean-definition-overriding: true
