server:
  port: 8082
  servlet:
    context-path: /
cors:
  allowed_origins:
    - "http://localhost:3000"

no.nav.security.jwt:
  issuer:
    selvbetjening:
      discoveryurl: http://localhost:${mock-oauth2-server.port}/selvbetjening/.well-known/openid-configuration
      accepted_audience: aud-localhost
      cookie_name: localhost-idtoken
    idporten:
      discovery-url: http://localhost:${mock-oauth2-server.port}/idporten/.well-known/openid-configuration
      accepted-audience: familie-app

FAMILIE_DOKUMENT_STORAGE_ENCRYPTION_PASSWORD: not-a-real-pw
FAMILIE_DOKUMENT_FNR_SECRET_SALT: detteErIkkeSaltMenHemmeligSalt

gcp.storage.bucketname: not-a-real-name

spring.main.allow-bean-definition-overriding": true